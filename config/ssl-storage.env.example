# SSL certificate storage (for reprovisioned servers)
# Copy to .env or export before running install. One predefined storage for restore + backup.
#
# --- Option A: Cloudflare R2 (S3-compatible, free tier) ---
# 1) Cloudflare Dashboard → R2 → Create bucket (e.g. odoo-ssl-certs)
# 2) R2 → Manage R2 API Tokens → Create API token (Object Read & Write) → copy Access Key ID + Secret
# 3) Account ID: Dashboard right sidebar or R2 overview URL
export ODOO_SSL_STORAGE=s3
export ODOO_SSL_S3_BUCKET=odoo-ssl-certs
export ODOO_SSL_S3_PREFIX=odoo-ssl
export ODOO_SSL_S3_ENDPOINT_URL=https://YOUR_ACCOUNT_ID.r2.cloudflarestorage.com
export AWS_ACCESS_KEY_ID=your_r2_access_key_id
export AWS_SECRET_ACCESS_KEY=your_r2_secret_access_key
#
# --- Option B: AWS S3 (or other S3-compatible) ---
# export ODOO_SSL_STORAGE=s3
# export ODOO_SSL_S3_BUCKET=my-bucket
# export ODOO_SSL_S3_PREFIX=odoo-ssl
# export AWS_ACCESS_KEY_ID=your_access_key
# export AWS_SECRET_ACCESS_KEY=your_secret_key
# For Oracle OCI: export ODOO_SSL_S3_ENDPOINT_URL=https://namespace.compat.objectstorage.region.oraclecloud.com
# For AWS S3 leave ODOO_SSL_S3_ENDPOINT_URL unset. For MinIO: .../http://minio:9000
#
# --- Option B: URL (restore from URL; backup to PUT URL or backup command) ---
# export ODOO_SSL_STORAGE=url
# export ODOO_SSL_RESTORE_URL=https://your-storage.example.com/ssl/your-domain/cert.tar.gz
# export ODOO_SSL_BACKUP_URL=https://your-upload-endpoint.example.com/ssl/your-domain/cert.tar.gz
# export ODOO_SSL_BACKUP_TOKEN=optional_bearer_token
#
# --- Option C: OneDrive / Microsoft 365 ---
# OneDrive is not directly accessible from the server. Options:
# 1) Use Azure Blob Storage (same Microsoft cloud): S3 API or Azure CLI; or
# 2) Run a small upload endpoint (e.g. Azure Function) that accepts PUT and saves to OneDrive/Blob,
#    then set ODOO_SSL_BACKUP_URL and ODOO_SSL_RESTORE_URL to that endpoint.
# 3) Sync from your laptop: backup script that pulls cert from server (scp) and uploads to OneDrive.
#
# --- Unset = local store only (optional ODOO_SSL_RESTORE_URL and ODOO_SSL_BACKUP_CMD) ---
